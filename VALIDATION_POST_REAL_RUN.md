# Post-real-run validation

Validates the fixes derived from the first real Tridi Meta run: recent-spend baseline, leave-one-out hierarchy, parent-child execution consistency, attribution-safe temporal handling, observability compilation, core tests, CLI smoke and deep smoke.
