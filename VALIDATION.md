# Validation

This file records the CI validation checkpoint for v0.5.0.

The test suite covers the lightweight hierarchical engine, temporal derivatives,
response elasticity, MCMC posterior moment matching, posterior overrides,
data-quality checks and module import smoke tests.
